# Direct VoxCPM runtime dependencies not already owned by vLLM, pinned from
# VoxCPM-main/uv.lock.
#
# vLLM 0.20.0 owns torch, torchaudio, transformers, huggingface-hub, pydantic,
# safetensors, tqdm, and einops. Do not pin them here or pip can downgrade the
# vLLM runtime after the vLLM wheel is installed.
addict==2.4.0
argbind==0.3.9
datasets==3.6.0
funasr==1.3.1
gradio==6.9.0
inflect==7.5.0
librosa==0.11.0
matplotlib==3.10.8
modelscope==1.35.0
simplejson==3.20.2
sortedcontainers==2.4.0
soundfile==0.13.1
spaces==0.47.0
torchcodec==0.10.0
wetext==0.1.2
